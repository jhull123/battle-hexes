// import { Board } from "../board";

export class CombatResolver {
  /*
  #board;
  #apiHost = 'http://localhost:8080';

  constructor(board) {
    this.#board = board;
  }

  resolve() {
    
  }

  setApiHost(apiHost) {
    this.#apiHost = apiHost;
  }
    */
}