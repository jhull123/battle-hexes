export class VillageDrawer {
  #p;
  #hexDrawer;

  constructor(p, hexDrawer) {
    this.#p = p;
    this.#hexDrawer = hexDrawer;
  }

  draw(aHex) {
    // TODO implement me!
    console.log(this.#p);
    console.log(this.#hexDrawer);
    console.log(aHex);
  }
}
